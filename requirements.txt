# Версии, на которых получены результаты главы 7 диссертации (Python 3.11, Linux x86-64, 2 ядра CPU, 23.09.2026).
# На другой ОС/процессоре подойдут и более новые версии; побитовое совпадение чисел ГНС тогда не гарантируется
# (см. README.md, раздел «Воспроизводимость и сверка»).
# Колёса этих версий для Mac с Apple Silicon есть для Python 3.11–3.13; torch 2.14.0, numpy 2.4.4 и scipy 1.17.1 требуют macOS 14+.
# Если macOS старше, уберите номера версий у этих трёх пакетов — pip подберёт совместимые (числа ГНС могут слегка отличаться).
numpy==2.4.4
scipy==1.17.1
pandas==3.0.2
scikit-learn==1.8.0
torch==2.14.0
torch_geometric==2.8.0.post1
matplotlib==3.10.9
certifi            # корневые сертификаты для скачивания данных (особенно на macOS)
tqdm==4.70.1        # индикатор прогресса run_all.py (уже тянется как зависимость torch_geometric)
