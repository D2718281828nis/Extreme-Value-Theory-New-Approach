# Проверка воспроизводимости (23.09.2026)

Проверялось, что расчёты главы 7 повторяются кодом этой папки с нуля и дают те же числа, что в исследовании (`reference_results/`).

## Условия

| Параметр | Значение |
|---|---|
| Где | Облачная рабочая среда Claude, Linux x86-64, 2 ядра Intel Xeon 2,1 ГГц |
| Окружение | Python 3.11.15; numpy 2.4.4; scipy 1.17.1; pandas 3.0.2; scikit-learn 1.8.0; torch 2.14.0 (CPU); torch_geometric 2.8.0.post1; matplotlib 3.10.9 |
| Потоков на процесс | 1 (`OMP_NUM_THREADS=1`, как задаёт `run_all.py`) |
| Данные примеров 2–3 | Скачаны заново `scripts/fetch_data.py` в пустую папку `data/`; MD5 всех четырёх файлов совпали |
| Данные примера 1 | Производный набор `sEEG-HFOs-8_cases_derived.npz` из `BioMedAI-sEEG-core-of-epilepsy/dataset/`; MD5 `6e768cf1…` совпал, записанный внутрь MD5 исходного EDF совпал со значением Zenodo |

## Результат сверки `scripts/compare_results.py`

| Пример | Детерминированные числа | Числа, зависящие от обучения ГНС | Время счёта |
|---|---|---|---|
| 1. Мозг | 231 из 231 | 96 из 96 | 77 с |
| 2. Двигатель | 315 из 315 | 152 из 152 | ГНС ≈ 28 мин (2 процесса), локализация ≈ 11 мин, сборка ≈ 1 мин |
| 3. Рынок | 332 из 332 | 126 из 126 | 762 с |

- **Числа.** Все 1252 числа `results.json` совпали с эталоном; допуск задан, но фактически совпадение точное.
- **Промежуточные файлы.** Все 19 CSV/NPY/NPZ совпали с эталоном побайтно: `gnn_runs*.csv`, `localization_*.csv`, `detection_FD001.csv`, `h1_windows.csv`, `h2_windows.csv`, `market_losses.csv`, `node_features.csv`, `indicators.npz`, `cascade.npz`, `hi_*.npy`.
- **Рисунки.** Рисунки 57–62, построенные `make_figures.py`, попиксельно совпали с файлами `disser-text/images/ch7_*.png`. Исключение — Рис. 60: там 77 пикселей из 2,96 млн отличаются на одну градацию яркости из-за сглаживания. Сами файлы PNG отличаются побайтно из-за сжатия.

## Что выяснилось при проверке

1. **Число потоков важно.** При двух потоках PyTorch на процесс средние AUROC/AUPRC ГНС примера 3 разошлись с эталоном до 0,002: одно значение, AUPRC «эпизод вне обучения, GATv2, секторный граф», изменилось во втором знаке — 0,54 → 0,53. При одном потоке совпадение полное. Отсюда следует, что исходный расчёт выполнялся по одному потоку на процесс; поэтому `run_all.py` по умолчанию задаёт `OMP_NUM_THREADS=1`. Детерминированные числа от числа потоков не зависят.
2. **Два процесса по два потока на двух ядрах** замедляют обучение ГНС примера 2 примерно в 15 раз: 8 с на эпоху вместо 0,5 с. Та же настройка `OMP_NUM_THREADS=1` это устраняет.
3. **Порядок запуска примера 2.** ГНС затравок 0 и 1–2 выполнялась через `run_all.py`. Стадии `--stage loc` и `--stage final` пришлось запустить теми же командами вручную с `OMP_NUM_THREADS=1`: настройка потоков была добавлена в `run_all.py` во время этой проверки.

## Что не проверено здесь

- **macOS (Apple Silicon) и другие ОС.** Детерминированная часть должна совпасть. Числа ГНС могут отличаться в третьем-четвёртом знаке из-за другой реализации линейной алгебры.
  - Колёса всех пакетов из `requirements.txt` для Python 3.11 и macOS arm64 существуют, но `torch 2.14.0`, `numpy 2.4.4` и `scipy 1.17.1` требуют macOS 14 или новее.
- **Построение производного набора стерео-ЭЭГ из EDF** (`fetch_data.py --only seeg` без готового `.npz`). Исходный EDF (743 МБ) сюда не переносился. Скрипт `src/brain_extract_edf.py` тот же, которым набор построен 23.09.2026 на компьютере автора.
- **Ссылка на скачивание EDF с Zenodo** (`--download-edf`) отсюда не открывалась.
- **Повторное обучение ГНС блока E § 3.11** (`scripts/block_e_commands.sh`) не выполнялось. `check_block_e.py` сверил с текстом сохранённые результаты репозитория автора: все 8 прогонов и статистика DFA совпали. Граф объекта сейчас содержит 98 узлов и 370 рёбер.
