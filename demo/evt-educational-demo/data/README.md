# Данные demo

`synthetic_data.csv` создаётся командой `python scripts/run_demo.py --seed 42` и содержит колонки `timestamp`, `value`, `is_extreme`. Это полностью синтетические данные без сведений о людях или реальных объектах.
